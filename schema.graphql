
type Book {
  title: String!
  author: String!
}
type Query {
  greeting: String
  getBooks: [Book]
  getAuthors: [String]
}
input BookInput {
  title: String!
  author: String!
}
type Mutation {
  addBook(input: BookInput!): Book
}
